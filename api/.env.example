# Database connection string
DATABASE_URL=postgresql://user:password@localhost:5432/nba_delays

# Secret key for FastAPI (sessions, JWT, etc.)
SECRET_KEY=j4yph3x

# NBA API key (if you’re hitting a 3rd-party API that needs one — else leave blank)
NBA_API_KEY=your_api_key_here

# Environment (dev, prod)
ENV=development

# CORS origins (comma-separated if multiple frontends)
CORS_ORIGINS=http://localhost:3000